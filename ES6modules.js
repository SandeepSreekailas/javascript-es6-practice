// ES6-Modules :- A module is a file containing a set of functions, variables, or classes. 
//                Modules are imported into other files to reuse their code.

import { pi, area,circumference}  from "./Mathutils.js" ;

console.log(pi);
console.log(area(6));
console.log(circumference(5));
